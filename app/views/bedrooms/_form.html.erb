<%= form_with(model: bedroom) do |form| %>
  <% if bedroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bedroom.errors.count, "error") %> prohibited this bedroom from being saved:</h2>

      <ul>
        <% bedroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :room/type_id %>
    <%= form.text_field :room/type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
