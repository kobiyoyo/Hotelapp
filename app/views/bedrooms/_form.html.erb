<%= form_for bedroom do |form| %>
  <% if bedroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bedroom.errors.count, "error") %> prohibited this bedroom from being saved:</h2>

      <ul>
        <% bedroom.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="form-group">
   <%= form.label 'Room Types' %>
   <%= form.collection_select(:room_type_id, Room::Type.all, :id, :name) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
